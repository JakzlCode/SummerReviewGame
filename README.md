To-do list
- Remove redundant grade level markers
- Keep the menu buttons in place
- Make the two Study/Test buttons indicators to show what mode the user is in currently and cannot be used to switch modes. Instead, the "start test" button redirects the user to the test mode (as usual) but when in the test mode, the "start test" buttons is replaced by a "go study" button.
- Add exclamation marks to the congratulatory message when passing a test.
- Make the progress bar in the right panel a different color when taking a test so it's not confused with the overall course progress bar.
- Fix the study/test missed words button after a test
- Accumilate a missed words list after all the tests and every time it hits 10 the user is forced to review the 15 missed words before progressing on to the next lesson. If by the final test there are some missed words review all of them before taking the final test.
